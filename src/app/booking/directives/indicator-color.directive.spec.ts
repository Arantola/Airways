import { IndicatorColorDirective } from './indicator-color.directive';

describe('IndicatorColorDirective', () => {
  it('should create an instance', () => {
    const directive = new IndicatorColorDirective();
    expect(directive).toBeTruthy();
  });
});
